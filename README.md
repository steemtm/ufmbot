# ufmbot

Setup:
pip install beem
pip install steemengine

setup a beem wallet:

beempy createwallet

add the posting key of the upvote account in the prompt:

beempy addkey

Start script:

python ufmbot.py
